import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.empresa.asistencia',
  appName: 'Control de Actividades',
  webDir: 'dist'
};

export default config;
